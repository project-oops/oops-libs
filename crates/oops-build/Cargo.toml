[package]
name = "oops-build"
description = "Which build this is: commit, version and time, stamped once and said the same way everywhere."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

# None. Every binary takes this crate as a dependency and a build-dependency, so anything
# added here compiles twice per tool.
[dependencies]

[lints]
workspace = true
